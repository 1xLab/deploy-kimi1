services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - VERSION=1.0.0
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/health"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s
