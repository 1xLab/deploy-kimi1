{
  "name": "deploy-kimi1",
  "version": "1.0.0",
  "description": "Minimal professional HTTP API with CI/CD, Docker and idempotent SSH deploy",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --coverage --verbose",
    "lint": "eslint src tests"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "license": "MIT"
}
